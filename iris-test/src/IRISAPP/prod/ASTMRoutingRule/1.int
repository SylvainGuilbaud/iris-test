ROUTINE IRISAPP.prod.ASTMRoutingRule.1 [Type=INT,Generated]
 ;IRISAPP.prod.ASTMRoutingRule.1
 ;Generated for class IRISAPP.prod.ASTMRoutingRule.  Do NOT edit. 09/15/2026 08:25:09PM
 ;;3153636D;IRISAPP.prod.ASTMRoutingRule
 ;
evaluateRuleDefinition(pContext,pRuleSet,pEffectiveBegin,pEffectiveEnd,pReturnValue,pReason,pLogLevel,pDebugId="") methodimpl { s:'($d(pLogLevel)#2) pLogLevel="r"
    set tSC=1,$ZE=""
    try {
        set pReturnValue="",pReason=""
        ; Initialize temporary variables
        ; No ruleSet dispatching code generated.
        set pEffectiveBegin=""
        set pEffectiveEnd=""
        set pRuleSet=""
        ;
ruleSet1 ; effectiveBegin = (not-specified); effectiveEnd = (not-specified)
        if (pLogLevel [ "c") set tSC = ##class(Ens.Rule.DebugLog).Log(pDebugId,"c","","Executing ruleSet1: effectiveBegin = (not-specified); effectiveEnd = (not-specified)",0) quit:('tSC)
        ;
        ; Evaluating rule: rule#1(équipement émetteur = COBAS 8000)
        kill tConstraints
        set tSC=##class(EnsLib.EDI.MsgRouter.SegmentedRuleAssist).RuntimeConstraintCheck(.tConstraints,pContext,.pPassed) quit:('tSC)
        if (pLogLevel [ "c") set tSC = ##class(Ens.Rule.DebugLog).Log(pDebugId,"c","rule#1(équipement émetteur = COBAS 8000)","Constraints for rule 'équipement émetteur = COBAS 8000' "_$select(pPassed: "passed",1: "failed"),0) quit:('tSC)
        if pPassed {
            if (((pContext.Document.GetValueAt("H:SenderNameOrId.SenderNameOrId"))=("COBAS8000"))) {
                set pReason=pReason_",rule#1(équipement émetteur = COBAS 8000):when#1"
                if (pLogLevel [ "c") set tSC = ##class(Ens.Rule.DebugLog).Log(pDebugId,"c","rule#1(équipement émetteur = COBAS 8000):when#1","When (Document.{H:SenderNameOrId.SenderNameOrId}=""COBAS8000"") succeeded",0) quit:('tSC)
                set pReturnValue=pReturnValue_$s(pReturnValue'="":";",1:"")_"send:vers ASTM E1394 - FICHIER:ASTM.transfo.E1394"
            } elseif (pLogLevel [ "c") {
                set tSC = ##class(Ens.Rule.DebugLog).Log(pDebugId,"c","rule#1(équipement émetteur = COBAS 8000)","For rule 'équipement émetteur = COBAS 8000', no whens matched and no otherwise defined",0) quit:('tSC)
            }
        }
        quit
        ;
    } catch err {
        if $ZE="" {set tSC=err.AsStatus()} Else {set tSC=##class(Ens.Util.Error).ExceptionStatus()}
    }
    set pReason=$e(pReason,2,*)
    quit tSC
    ;
}