ROUTINE IRISAPP.prod.LISRoutingRule.1 [Type=INT,Generated]
 ;IRISAPP.prod.LISRoutingRule.1
 ;Generated for class IRISAPP.prod.LISRoutingRule.  Do NOT edit. 04/17/2026 03:08:31PM
 ;;384A4942;IRISAPP.prod.LISRoutingRule
 ;
evaluateRuleDefinition(pContext,pRuleSet,pEffectiveBegin,pEffectiveEnd,pReturnValue,pReason,pLogLevel,pDebugId="") methodimpl { s:'($d(pLogLevel)#2) pLogLevel="r"
    set tSC=1,$ZE=""
    try {
        set pReturnValue="",pReason=""
        ; Initialize temporary variables
        ; No ruleSet dispatching code generated.
        set pEffectiveBegin=""
        set pEffectiveEnd=""
        set pRuleSet=""
        ;
ruleSet1 ; effectiveBegin = (not-specified); effectiveEnd = (not-specified)
        if (pLogLevel [ "c") set tSC = ##class(Ens.Rule.DebugLog).Log(pDebugId,"c","","Executing ruleSet1: effectiveBegin = (not-specified); effectiveEnd = (not-specified)",0) quit:('tSC)
        ;
        ; Evaluating rule: rule#1(ASTM E1394)
        kill tConstraints
        set tConstraints("source")="de LIS ASTM E1394"
        set tSC=##class(EnsLib.MsgRouter.RuleAssist).RuntimeConstraintCheck(.tConstraints,pContext,.pPassed) quit:('tSC)
        if (pLogLevel [ "c") set tSC = ##class(Ens.Rule.DebugLog).Log(pDebugId,"c","rule#1(ASTM E1394)","Constraints for rule 'ASTM E1394' "_$select(pPassed: "passed",1: "failed"),0) quit:('tSC)
        if pPassed {
            if ((1)) {
                set pReason=pReason_",rule#1(ASTM E1394):when#1"
                if (pLogLevel [ "c") set tSC = ##class(Ens.Rule.DebugLog).Log(pDebugId,"c","rule#1(ASTM E1394):when#1","When (1) succeeded",0) quit:('tSC)
                set pReturnValue=pReturnValue_$s(pReturnValue'="":";",1:"")_"send:vers LIS ASTM E1394:"
                set pReturnValue=pReturnValue_$s(pReturnValue'="":";",1:"")_"send:vers BASE PATIENT:IRISAPP.transfo.ASTME1394"
            } elseif (pLogLevel [ "c") {
                set tSC = ##class(Ens.Rule.DebugLog).Log(pDebugId,"c","rule#1(ASTM E1394)","For rule 'ASTM E1394', no whens matched and no otherwise defined",0) quit:('tSC)
            }
        }
        ; Evaluating rule: rule#2(ASTM E1238)
        kill tConstraints
        set tConstraints("source")="de LIS ASTM E1238"
        set tSC=##class(EnsLib.MsgRouter.RuleAssist).RuntimeConstraintCheck(.tConstraints,pContext,.pPassed) quit:('tSC)
        if (pLogLevel [ "c") set tSC = ##class(Ens.Rule.DebugLog).Log(pDebugId,"c","rule#2(ASTM E1238)","Constraints for rule 'ASTM E1238' "_$select(pPassed: "passed",1: "failed"),0) quit:('tSC)
        if pPassed {
            if ((1)) {
                set pReason=pReason_",rule#2(ASTM E1238):when#1"
                if (pLogLevel [ "c") set tSC = ##class(Ens.Rule.DebugLog).Log(pDebugId,"c","rule#2(ASTM E1238):when#1","When (1) succeeded",0) quit:('tSC)
                set pReturnValue=pReturnValue_$s(pReturnValue'="":";",1:"")_"send:vers LIS ASTM E1238:"
                set pReturnValue=pReturnValue_$s(pReturnValue'="":";",1:"")_"send:vers BASE PATIENT:IRISAPP.transfo.ASTME1238"
            } elseif (pLogLevel [ "c") {
                set tSC = ##class(Ens.Rule.DebugLog).Log(pDebugId,"c","rule#2(ASTM E1238)","For rule 'ASTM E1238', no whens matched and no otherwise defined",0) quit:('tSC)
            }
        }
        quit
        ;
    } catch err {
        if $ZE="" {set tSC=err.AsStatus()} Else {set tSC=##class(Ens.Util.Error).ExceptionStatus()}
    }
    set pReason=$e(pReason,2,*)
    quit tSC
    ;
}